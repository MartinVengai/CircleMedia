export class Source {
  constructor(id?: number, name?: string) {
    this.id = id;
    this.name = name;
  }

  id: number;
  name: string;
}

export class Status {
  constructor(id?: number, name?: string) {
    this.id = id;
    this.name = name;
  }

  id: number;
  name: string;
}